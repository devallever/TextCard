package com.text.card.core

import com.text.card.R

class WordCountFormat {
    companion object {
        val FORMAT_WORD_COUNT = R.string.word_format_word_count
        val FORMAT_COUNT_WORD = R.string.word_format_count_word
        val FORMAT_COUNT = R.string.word_format_count

        val FORMAT_LIST = mutableListOf(
            FORMAT_WORD_COUNT,
            FORMAT_COUNT_WORD,
            FORMAT_COUNT
        )
    }
}